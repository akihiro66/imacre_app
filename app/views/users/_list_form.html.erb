<div id="list-<%= @production.id %>" class="list-button">
  <% if !current_user.nil? && current_user.list?(@production) %>
    <%= render 'users/unlist' %>
  <% else %>
    <%= render 'users/list' %>
  <% end %>
</div>
